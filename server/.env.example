# Hostinger MySQL connection (from hPanel → Databases)
MYSQL_HOST=your-hostinger-mysql-host.mysql.services.com
MYSQL_PORT=3306
MYSQL_DATABASE=u792097907_slughouse
MYSQL_USER=u792097907_tdv
MYSQL_PASSWORD=your-secure-password
MYSQL_CONN_LIMIT=10

# API server configuration
PORT=4000

# CORS: comma-separated list of allowed frontend origins
ALLOWED_ORIGINS=https://playback.slughouse.com,http://localhost:3000

# Optional: Require this token in `x-admin-token` header for POST/DELETE
# Must match VITE_ADMIN_TOKEN in frontend .env if set
ADMIN_TOKEN=

# File upload storage
# MEDIA_ROOT: Absolute path where audio files are stored (must be writable)
# MEDIA_BASE_URL: Public HTTPS URL that maps to MEDIA_ROOT (include trailing slash)
# MAX_UPLOAD_BYTES: File size limit in bytes (default 25 MB)
MEDIA_ROOT=/home/u792097907/uploads
MEDIA_BASE_URL=https://playback.slughouse.com/uploads/
MAX_UPLOAD_BYTES=26214400
