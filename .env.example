# Frontend Environment Variables

# Base URL for the Express API (include protocol, no trailing slash)
# Development: http://localhost:4000
# Production: https://api.playback.slughouse.com
VITE_API_BASE_URL=http://localhost:4000

# Optional: Shared secret that the backend expects in the `x-admin-token` header
# Leave blank if backend ADMIN_TOKEN is not set
VITE_ADMIN_TOKEN=
